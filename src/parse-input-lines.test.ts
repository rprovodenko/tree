import { parseInputLines } from "./parse-line/parse-input-line"

describe("parseInputLines", () => {
    it("parses CREATE", () => {
        expect(parseInputLines("CREATE fruits")).toEqual({

        })
    })
})
